export type SignUpBody = {
    name: string;
    email: string;
    key: string;
    secret: string;
};

export type SignUpResponse = {
    name: string;
    email: string;
    key: string;
    secret: string;
};
